apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-delete-tasks-svc
spec:
  selector:
    app: {{ .Release.Name }}-app-delete-task
  ports:
  - port: {{ .Values.deletetask.servicePort }}
    targetPort: {{ .Values.deletetask.targetPort }}
