apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-deployment-delete-task
spec:
  replicas: {{ .Values.deletetaskreplicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-app-delete-task
  template:
    metadata:
      name: demo-pod
      labels:
        app: {{ .Release.Name }}-app-delete-task
    spec:
      containers:
      - name: {{ .Release.Name }}-container
        image: {{ .Values.common.ContainerRegistryUrl }}/{{ .Values.deletetask.image.name }}:{{ .Values.deletetask.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.deletetask.targetPort }}
        env:
        - name: CONNECTION_STRING
          value: {{ .Values.common.ConnectionString }}
  strategy:
    type: RollingUpdate